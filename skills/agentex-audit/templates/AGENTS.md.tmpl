# AGENTS.md

<!--
This is a scaffold. Fill in every <placeholder>, then delete this comment
and any sections that don't apply. Target length: 80-200 lines. If this
file grows past 200 lines, move deeper detail into `docs/` or a CODEMAP.md.

The six sections below are the "top-tier" structure identified by
GitHub's analysis of 2,500+ AGENTS.md files (github.blog, 2025):
build/test commands, project structure, code style, git workflow,
testing expectations, and agent accessibility.
-->

## Project overview

<1-3 sentences: what the project is, who uses it, the stack
(language + framework + datastore + key libs with versions).>

Start here: read [`CODEMAP.md`](./CODEMAP.md) for the directory tour and
"where to add X" table before making any edits.

## Build, test, and run commands

Use these exact commands — wrapped in backticks so agents can execute
them directly:

- Install dependencies: `<command>`
- Run the dev server: `<command>`
- Run the full test suite: `<command>`
- Run fast unit tests only: `<command>`
- Run linter / formatter: `<command>`
- Run type checker: `<command>`
- Build for production: `<command>`

## Project structure

- `<top-level-dir>/` — <one-line purpose>
- `<top-level-dir>/` — <one-line purpose>
- ...

For the full directory tree and the "where to add X" table, see
[`CODEMAP.md`](./CODEMAP.md).

## Code style and conventions

- **Language version:** <e.g., Python 3.12, Node 20, Go 1.22>
- **Formatter:** <tool + config location>
- **Linter:** <tool + config location>
- **Type checker:** <tool + config location>
- **Naming:** <camelCase | snake_case | PascalCase — be explicit>
- **Imports:** <grouping, sorting rules>
- **Tests:** <naming, location, framework>
- **Comments:** <when to add them; prefer self-documenting code>

## Git workflow

- **Main branch:** `<main | dev>`
- **Feature branches:** `<naming pattern, e.g., feat/<short-desc>>`
- **Commit messages:** <convention, e.g., conventional commits>
- **PRs:** <review requirements, checks that must pass>
- **Never:** <list destructive actions that are off-limits: force-push
  to main, amend published commits, etc.>

## Testing expectations

- <What kind of tests exist and when each runs.>
- <The bar for "done": all tests green, coverage target, etc.>
- <Known flaky tests or skip conditions.>
- <How to add a test for a new feature — scaffold or pattern.>

## Agent accessibility and boundaries

### Do

- Read `CODEMAP.md` before editing.
- Run the full test suite before declaring work complete.
- Ask before deleting files or running destructive commands.

### Do NOT modify

- `<path>` — <reason (generated, vendored, legal, etc.)>
- `<path>` — <reason>
- Database migrations older than the most recent.

### Known gotchas

- <Cosmetic warnings that look like errors.>
- <Environment variables that must be set for local development.>
- <Hardcoded values that should be config-driven — link to issue if any.>

## Environment variables

All required variables are listed in [`.env.example`](./.env.example)
with comments describing what each one does. Agents should read that
file before first run.

## Further reading

- [`README.md`](./README.md) — human-facing pitch and install
- [`CODEMAP.md`](./CODEMAP.md) — directory tour and endpoint index
- [`docs/adr/`](./docs/adr/) — architecture decision records
- [`docs/runbook.md`](./docs/runbook.md) — operational recipes
