# CLAUDE.md

<!--
Target length: ≤ 200 lines. Start small; add rules only when actual
mistakes prove a rule is needed. Claude Code auto-loads this file on
session start, so every line is paid for on every session.

If a rule belongs in AGENTS.md (project overview, build commands,
structure, style, git workflow, testing, boundaries) put it there
and link. This file is for Claude-Code-specific behavior only.
-->

**Start here:** read [`CODEMAP.md`](./CODEMAP.md) and [`AGENTS.md`](./AGENTS.md)
before making any edits. CODEMAP has the directory tour; AGENTS has
commands, conventions, and boundaries.

## Work tracking

<!-- Replace with your project's actual tracker. Examples: bd, Linear,
GitHub Issues, Jira. -->

- Use `<tracker>` for ALL task tracking — do NOT use TodoWrite,
  ad-hoc markdown TODO lists, or scratch files.
- Use `<tracker>` for persistent knowledge too — do NOT create
  ad-hoc `MEMORY.md` files.

## Non-interactive shell commands

**ALWAYS use non-interactive flags** with file operations to avoid
hanging on confirmation prompts. On some systems `cp`, `mv`, `rm` are
aliased to `-i`.

```bash
cp -f source dest           # NOT: cp source dest
mv -f source dest           # NOT: mv source dest
rm -f file                  # NOT: rm file
rm -rf directory            # NOT: rm -r directory
```

Other commands that may prompt: `ssh`/`scp` (use `-o BatchMode=yes`),
`apt-get` (`-y`), `brew` (`HOMEBREW_NO_AUTO_UPDATE=1`).

## Development principles

- Think before acting. Read existing files before writing code.
- Be concise in output but thorough in reasoning.
- Prefer editing over rewriting whole files.
- Do not re-read files you have already read in this session.
- Test your code before declaring done.
- No sycophantic openers or closing fluff.
- Keep solutions simple and direct.
- User instructions always override this file.

## Session completion

**When ending a work session**, work is NOT complete until `git push`
succeeds.

1. **File issues for remaining work** — create `<tracker>` items for
   anything that needs follow-up.
2. **Run quality gates** (if code changed) — tests, linters, type check,
   build.
3. **Update issue status** — close finished work, update in-progress.
4. **Push to remote** (mandatory):
   ```bash
   git pull --rebase
   git push
   git status  # MUST show "up to date with origin"
   ```
5. **Clean up** — clear stashes, prune stale branches.
6. **Hand off** — provide context for the next session.

Never stop before pushing — that leaves work stranded locally.

## Known gotchas

<!-- Populate with cosmetic warnings, flaky commands, or things that
look like errors but aren't. Example: -->
<!--
- `<command>` prints `<scary-looking-message>` on every run. This is
  **cosmetic** — <real state>. Safe to ignore unless you're
  specifically `<edge case>`.
-->
